# Version ranges (not hard pins) so pip picks builds with prebuilt wheels for
# your Python version — including Python 3.13 and 3.14. Plain `uvicorn` (not
# uvicorn[standard]) avoids optional C/Rust extras that lag on new Pythons.
fastapi>=0.115,<1.0
uvicorn>=0.30,<1.0
httpx>=0.27,<1.0
sqlalchemy>=2.0.30,<3.0
apscheduler>=3.10,<4.0
pydantic>=2.11,<3.0
pydantic-settings>=2.7,<3.0
jinja2>=3.1,<4.0
python-dotenv>=1.0,<2.0
py-clob-client>=0.34,<1.0

# dev / test
pytest>=8.0
pytest-asyncio>=0.24
respx>=0.21
